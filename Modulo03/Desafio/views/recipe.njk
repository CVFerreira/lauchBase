{% extends "layout.njk" %}
{%  block content %}

    <div class="acessadas">
            <div class="content">
               <div class="card">
                    <img src="{{item.image}}" alt="{{item.title}}">
                    <h3>{{item.title}}</h3>
                    <h4>{{item.author}}</h4>
                    <h4>{{item.ingredients}}</h4>
                </div>
            </div>
    </div>

{% endblock content %}}

        
      